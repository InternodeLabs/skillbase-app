# ---- Central auth (portal-frontend) integration ----

# Base URL of the portal-frontend central auth authority.
#   local dev : http://localhost:3000
#   stage     : https://app.internode.us
#   production: https://app.internode.ai
PORTAL_BASE_URL=http://localhost:3000

# Secret used to sign this app's session cookie (HMAC-SHA256).
# Generate with: openssl rand -base64 32
SESSION_SECRET=change-me-generate-a-long-random-value

# Optional: force the public origin used to build the OAuth redirect_uri.
# Leave unset locally (the request origin is used). On Vercel, VERCEL_URL is
# used automatically; set APP_URL to a stable custom domain in production.
# APP_URL=https://skillbase.example.com

# ---- Database (Neon Postgres) ----

# Postgres connection string.
#   Local dev : use docker-compose.yml -> postgres://skillbase:skillbase@localhost:5433/skillbase
#   Production: Neon (Vercel Marketplace integration sets this automatically)
# The driver is chosen from the host: Neon hosts use neon-http, everything else
# uses node-postgres. Force it with DB_DRIVER=pg|neon if needed.
DATABASE_URL=postgres://skillbase:skillbase@localhost:5433/skillbase
